# build
dist/
.astro/

# 3D model sources — raw + textured Meshy exports, large (~30–40 MB each).
# The optimised, served copies in public/models/ ARE committed; these big
# originals stay local (re-optimise via scripts/optimize-models.mjs).
assets/glbs/
assets/models/

# deps
node_modules/

# environment
.env
.env.production
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor
.vscode/
.idea/
.DS_Store

# macOS
.AppleDouble
.LSOverride

# tmp
*.swp
*.swo
.tmp-screenshots/

# local Claude Code settings (personal, machine-specific)
.claude/settings.local.json
